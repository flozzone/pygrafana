python grafana backend
======================

This project aims to build a python backend for grafana, because grafana depends on go and other software
components, which are not easy to cross compile for embedded hardware.